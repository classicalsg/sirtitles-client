"use strict";

var _reactHooksTestingLibrary = require("react-hooks-testing-library");

var _ = require("../");

test('should increment counter', () => {
  const {
    result
  } = (0, _reactHooksTestingLibrary.renderHook)(() => (0, _.useLocalStorage)('test', 'Test'));
  expect(result.current[0]).toBe('Test');
  (0, _reactHooksTestingLibrary.act)(() => result.current[1]('Bob'));
  expect(result.current[0]).toBe('Bob');
});